// Legacy build-time source rewriting was removed.
// Application source must be updated directly; production builds must never mutate
// TypeScript/React source files as a side effect of npm run build.
console.log("NCR build patch: no-op (source rewriting disabled).");
